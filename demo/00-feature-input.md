# Feature Input (set once)

Replace these two placeholders once per demo run, then keep this file as the shared input for all 3 phases.

```text
FEATURE_REQUEST={{FEATURE_REQUEST}}
FEATURE_FOLDER={{FEATURE_FOLDER}}  # kebab-case, e.g. live-rooms
```
